import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    primary: {
      // pink
      main: "#FFA1F5",
      contrastText: "#fff",
    },
    secondary: {
      // green
      main: "#00DFA2",
    },
    info: {
      // yellow
      main: "#fff",
    },
  },
});
