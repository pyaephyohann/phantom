import { Skeleton } from "@mui/material";

const OrderCardSkeletonMobile = () => {
  return (
    <Skeleton animation="wave" variant="rounded" width={400} height={100} />
  );
};

export default OrderCardSkeletonMobile;
